# gaa-dashboard — 작업 컨텍스트

GLIF 26-2 자산운용팀 실시간 포트폴리오 대시보드. Next.js 16 + React 19.
이 파일은 **다른 기기·다른 계정에서 이 저장소를 열었을 때 설명 없이 이어서 작업하기 위한 인수인계 메모**다.

---

## 배포 규칙 (먼저 읽을 것)

- 프로덕션: https://gaa-dashboard-eight.vercel.app (`?team` 쿼리로 팀 전용 탭 활성화)
- Vercel 계정 `huey1270@gmail.com`, 팀 slug `huey1270-4778s-projects`, GitHub `ryuwise1/gaa-dashboard`
- **배포 경로는 GitHub `main` push 하나로 통일한다.** `vercel deploy`(CLI)나 배포 API를 직접 쓰면
  저장소가 프로덕션보다 뒤처지고, 그 상태에서 누가 push하면 **프로덕션이 옛 버전으로 롤백된다.**
  2026-08-03에 실제로 이 사고가 났고(4종목 소실·총액 감소), 8월 27일에 배포본에서 소스를 복구해 되돌렸다.
- 부득이하게 CLI/API로 배포했다면 **같은 소스를 즉시 `main`에 push**해서 저장소를 맞춰라.
- push 전 Vercel Deployments에서 최신 프로덕션이 Git 커밋(source=git)인지 확인할 것.

## 팀 모드 비밀번호 (2026-09-08 도입)

`?team` 첫 진입 시 비밀번호를 묻는다 (현재 **glif19gi**, 9/8 사용자 지정). 인증되면 localStorage에 저장돼 그 브라우저는 계속 팀 모드.
검증은 `components/Dashboard.tsx`의 SHA-256 해시 비교 — 소프트 게이트라 번들 분석으로 데이터 접근은 가능함(학회 내부용으로 충분).
비밀번호 변경법: `node -e "crypto.subtle.digest('SHA-256', new TextEncoder().encode('새비번')).then(b=>console.log([...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')))"` 로 해시를 만들어 Dashboard.tsx의 해시 상수를 교체하고 push.

## 매매 승인 규칙 (2026-09-08 신설, 사용자 지시 — 절대 규칙)

**`trades.json`에 매수·매도 기록을 추가하는 것은 사용자의 명시적 승인이 있을 때만 한다.**
분석·제안·규모 산정까지만 하고, 판단이 나오면 ① 근거와 규모를 제시하고
② `actions.json`(오늘 할 일 배너)에 "집행 예정"으로 올린 뒤 ③ 사용자 신호("집행해", "매수해줘" 등)를 기다린다.
"확인했어" 같은 분석에 대한 리액션은 승인이 아니다. 2026-09-07에 리뷰 결과를 승인 없이
원장에 기록했다가 9/8에 삭제·복원한 사고가 배경이다.

## 작업 로그 — 기기 간 팔로업 (최신순)

- **2026-09-09 오후 (메인 기기, UI 일괄 정비 — 사용자 요청 14건)** — ① 기본 표시 통화 **달러**(localStorage 키 `gaa-unit2`로 교체해 옛 원화 선호 무시), 현재가/평가금은 저장 안 함(항상 평가금으로 진입) ② **VT·SGOV → 섹터 "현금성 자산"**(코어 인덱스·현금 섹터 폐지, `SECTOR_SLOT` 갱신, 시그널은 SGOV만 티커로 제외), **VT 목표 40% 복원** — 합계 100%는 보류 확정된 KRE 슬롯 1.25→1.0%로 맞춤(SGOV는 5% 유지: 낮추면 배당 재투자분이 "축소 미집행"으로 오탐된다) ③ **스토리 블록 2단 구조** — 알파(메모리 15 + 보안 4) / 헤지(역상관 10 + 금리 12.5) / 분산(에너지 10 + 방산 3.75) / 현금성 44.75 네 묶음, 바는 묶음 색 농담으로 슬리브 구분. 4%짜리 위성 슬리브가 단독 행으로 떠 기괴해 보이던 문제의 해법 ④ **종목 정체 설명**: holdings.json 전 25종목에 `industry`·`about` 추가(워크플로 5에이전트 생성) — 플랜 근거 펼침 상단, 보유 현황 펼침 상단, AP·MP 펼침에 표시 ⑤ **AP·MP**: MP 내림차순 정렬(현금 맨 아래), 해외 종목은 **티커 기본 표시**·클릭 시 풀네임·산업·근거 펼침, **헤더 정렬 수정**(첫 열 로고 폭 29px 들여쓰기, 괴리 셀을 flex→grid로 바꿔 숫자 우측 끝 고정, 잔여/초과는 둘째 줄) ⑥ 플랜 탭 섹터·종목 목표 비중 내림차순 ⑦ **캘린더**: 국가명 → **국기 이미지**(flagcdn — Windows는 국기 이모지가 글자로 깨짐), 월간 클릭 상세의 어닝 종목 **로고**(Logo에 `any` prop — 알파벳 티커면 화이트리스트 밖도 CDN 시도), 주간·월간 그리드 자체엔 로고 없음 ⑧ **회의록**: 기본 전부 접힘, 배경/논의/결정 라벨 위계, 산문 측정폭 680px, 원형숫자(①~) 항목은 불릿 제거+내어쓰기, 결정 문단을 ①/문장 단위로 분해, 성적표 로고 ⑨ 감사에서 발견한 버그 3건: 모바일 플랜 행 열 수 불일치(목표금액 줄바꿈), 보유 리스트 구분선 셀렉터 무효(`.hold + .hold`), 섹터 칩 미축소. **대기**: 오늘 15:30 코스피 종가 → 회의 → 하이닉스 증액 여부(사용자 집행 신호 필요) · VT 축소 안건 · 6차 회의록.
- **2026-09-09 (메인 기기, AP·MP 결함 수정)** — 전날 예약 작업이 남긴 숙제 처리. **`축소 미집행` 원인 분류 추가** (`Allocation.tsx`) — 취득원가가 목표를 5% 넘게 초과하면(=목표를 낮췄는데 아직 안 팔았다) 드리프트가 아니라 축소 미집행으로 잡는다. `excessUsd` 필드 신설, 행에 "초과 $X" 표시, 헤더에 **축소 대기** 합계를 미체결 목표와 **분리** 표기(방향이 반대라 절대 합치지 않는다), 용어 설명 한 줄 추가, `.alloc-cause.trim` 앰버 칩. EUAD 목표를 임시로 1%로 낮춰 분류·표시·합계를 검증하고 원복. ⚠️ 세션 시작 시 fetch 규칙도 위에 신설(9/9 오판 사고). **대기**: RBRK·CRWD 2차 · 삼성전자 밴드 이탈 결정 · 6차 회의록 · 미 10년물 4.806%(룰 5.0%까지 0.19%p).
- **2026-09-08 밤 (예약 작업 `gaa-exec-0908`, 미국장 개장 후 집행)** — 개장 확인(정규장 09:3x ET, 프리장 아님) 후 예약된 3건을 실제 시세로 원장 기록. ① **EUAD 397주 매도 @ $43.80** (회수 $17,388.60, 실현손익 **−$1,348.49 / −7.20%**, 평단 $47.1967) — 절반 축소 확정, 잔여 398주 유지. ② **RBRK 68주 매수 @ $91.07** ($6,192.76, 예산 $6,250) — `status:"매수중"`, 1차 절반. ③ **CRWD 17주 매수 @ $210.03** ($3,570.51, 예산 $3,750) — `status:"매수중"`, 1차 절반. 환율 1,341.08원/$. `check-ledger.mjs` 통과, `npm run build` 통과. `actions.json`에서 `euad-trim`·`cyber-first-half` 삭제. **대기**: RBRK·CRWD 2차(−5% 눌림 또는 Fal.Con 소화 후) · 삼성전자 밴드 이탈 익절 여부 팀 결정 · AP·MP `축소 미집행` 원인 분류 추가(아래 항목).
- **2026-09-08 밤 (외부 기기, 집행 예약 + 정합성 검사기)** — ① 사용자 지시로 **오늘 집행 3건(EUAD 397주 매도 · RBRK $6,250 · CRWD $3,750)을 미국장 개장 후 실제 시세로 기록하도록 예약** (Claude 예약 작업 `gaa-exec-0908`, 22:35 KST 1회). 개장 전 프리장 단계라 종가로 대체 기록하지 않았다 — 9/4(금) 이후 노동절 휴장이 끼어 갭 가능성이 커서, 잘못된 평단이 수익률·비중·기여도로 번지는 것을 피했다. ② **`tools/check-ledger.mjs` 신설** — `holdings.json`의 qty·avgPrice가 원장 리플레이와 일치하는지 검사한다. 둘은 자동 동기화되지 않고(화면 평가액은 holdings.qty, 취득원가는 원장) 어긋나면 조용히 틀어지므로 **매매 기록 후 반드시 실행할 것**. 현재 전부 일치. ③ ⚠️ **AP·MP 표 결함 발견** — 목표를 낮추고 매도를 안 한 종목(지금 EUAD)이 `드리프트`로 분류된다. `Allocation.tsx`가 매수 방향만 봐서(`spent >= targetUsd`면 드리프트) **축소 미집행을 못 잡는다**. `축소 미집행` 원인 추가 필요 — 집행 후 작업.
- **2026-09-08 밤 (메인 기기, 하이닉스 매수 취소)** — 오전 임시회의로 기록했던 **SK하이닉스 6주 매수(₩1,858,000)와 목표 상향(3.0→4.5%)을 사용자 지시로 전부 취소**. 사유: 편입 시 반증 조건이었던 **코스피 7,000 종가 유지가 실패** — 조건이 당일 발동된 셈이라 증액 근거 소멸. 하이닉스 15주·평단 1,393,500원·목표 3.0% 복원, VT 목표 39.75%로 재조정(합계 100%), 스토리 블록·운용 리뷰 페이지에서 상향 문구 제거.

> **규칙 (2026-08-28 신설, 사용자 지시)**: 어느 기기에서든 편집을 마치면
> ① `main`에 push하고 ② **아래 로그에 한 항목을 최신순으로 추가**한 뒤 push에 포함시킨다.
> 형식: 날짜 · 한 일 · 보류/대기 · 다음 작업. 상세는 커밋 메시지가 정본이고,
> 보드 팀 모드(?team) 하단 **"보드 변경 로그"** 섹션에서 최근 커밋을 사람이 바로 볼 수 있다.
> 로그는 최근 10개 안팎만 유지 (오래된 건 삭제).
>
> ⚠️ **세션 시작 시 `git fetch origin && git pull --ff-only origin main`을 먼저 실행한 뒤 이 로그를 읽는다** (2026-09-09 신설).
> 로컬 파일만 보면 다른 기기·예약 작업이 이미 처리한 일을 "미처리"로 잘못 보고하게 된다 —
> 9/9에 실제로 집행 완료된 3건을 지연으로 오판한 사고가 있었다. 원격이 앞서 있으면 pull 후 원장 검사기
> (`node tools/check-ledger.mjs`)로 정합성을 확인하고 시작할 것.

- **2026-09-08 밤 (메인 기기, 시세 버그 + 배당 재투자 + 오버레이)** — ① **국내 "시간외" 오표기 수정**: 네이버가 정규장 중에도 NXT 병행 세션을 OPEN으로 내려보냄 — 평일 09:00~15:30 KST엔 over를 싣지 않음 (`api/quotes`). ⚠️ **미국 주간거래(데이마켓, Blue Ocean) 시세는 무료 소스가 없다** — 야후 v7 quote(crumb)도 overnight 필드 미제공 확인. 마감 후엔 애프터만 표시 ② **배당 자동 재투자 정책 채택(사용자 승인)**: `data/dividend-ledger.json`(수령 확정분, 현재 12건 $743) 합계가 현금에 가산되고(`lib/trades.ts` dividendUsd), 월 1회 SGOV 일괄 매수로 원장 기록 — 1회차 SGOV 7주 매수 기록 완료. **규칙: 주간 점검 때 /api/dividends와 ledger를 대조해 신규 수령분 추가** ③ **캘린더 우리 활동 오버레이** — 회의·매매·운용 개시를 그리드에 표시, 날짜 클릭 상세에 "우리 활동" 블록(체결 note 포함) ④ 오늘 할 일 개조식(bullets 배열), 스토리 불릿 키워드 볼드.
- **2026-09-08 저녁 (메인 기기, UI 정리 + 캘린더 확장 + 배당)** — ① **투자 스토리**: 기본 접힘 + 역할별 비중 스택 바·범례 상시 표시, 펼치면 근거 개조식 (`StoryBlock.tsx` 전면 개편) ② **캘린더 7~12월 전체**: 과거는 Nasdaq 스크랩(`data/macro-history.json`, 실제치·판정 포함 — **낫닥 date 파라미터는 발표일+1이라 쿼리일−1로 보정, 주말 쿼리 필수(금요일 고용보고서가 토요일 쿼리에 실림)**), 미래는 공식 일정 큐레이션(`data/macro-schedule.json`, FOMC·BLS·BEA·ECB·BOE·BOJ — 소매판매·PMI속보 등 일부 근사, 발표 주가 되면 FF 피드가 대체·dedupe). 월간이 기본 뷰, 앞뒤 달 프리페치, **캘린더 탭 공개 전환**(주간 표 만들기는 팀 전용 유지) ③ **배당 수익 기능**(`/api/dividends` + `Dividends.tsx`, 매매 내역 탭 하단): 야후 배당 이벤트 × 원장 리플레이 수량, 배당락일 기준, 연 수익률·지급주기 표시. 현금 잔고와 별도 집계 — 원장 반영 방식은 회의에서 결정 ④ 오늘 할 일 배너 문구 정리, AP·MP 설명 한 줄+접힘, 수익 기여 전체 펼침. 스크랩 재생성 스크립트는 세션 스크래치패드 `scrape-macro.js` (재사용 시 위 낫닥 함정 참조).
- **2026-09-08 오후 (메인 기기, 정정 + 스토리 반영)** — ① **9/7 매도 2건(삼전 24주·하이닉스 3주) 원장에서 삭제** — 사용자 승인 없이 기록된 건이라 취소 지시받음. 삼전 103주·하이닉스 21주(평단 1,526,214원)로 복원. 아래 "매매 승인 규칙" 신설 ② **AI 보안 슬리브를 플랜·스토리에 정식 반영** — holdings에 RBRK(목표 2.5%)·CRWD(1.5%) 미매수로 추가, `SECTOR_SLOT`에 "AI 보안"(+`--s9` 색), 스토리 블록에 "알파 ②" 역할 추가 ③ **MP 재배분**: EUAD 7.5→3.75%(절반 축소 반영), VT 40→38.25%(신규 슬리브 재원), 목표 합계 100% 유지 ④ 삼전이 밴드 상한 이탈 상태로 AP·MP에 보일 것 — 익절 여부는 팀 결정 대기.
- **2026-09-08 오전 (메인 기기, 임시회의 집행)** — 카톡 임시회의(김서연·김진웅 전원 찬성) 결정 3건. ① ~~SK하이닉스 6주 매수·목표 상향~~ → **당일 밤 취소** (반증 조건 발동 — 위 항목 참조) ② **AI 보안 1차 매수 승인** — RBRK $6,250 + CRWD $3,750, 오늘 밤 미국장 개장 시 집행 (촉매: GPT-6 Astra 'Critical' 등급 9/3) ③ EUAD 397주 손절 확정. **오늘 밤 할 일: 사용자 "집행해" 신호 후 EUAD 매도 → RBRK·CRWD 매수 기록 + actions.json에서 두 항목 제거.**
- **2026-09-08 (메인 기기)** — 팀 뷰 편의 기능 4종. ① **"오늘 할 일" 배너 신설** (`components/ActionBar.tsx` + `data/actions.json`, 팀 전용) — 예약된 집행·감시 룰·기록 대기를 탭 위에 상시 표시. **규칙: 집행·결정이 끝나면 actions.json에서 그 항목을 지우고, 새 예약·룰이 생기면 추가한다** (현재 3건: EUAD 9/8 집행·TLT 5.0% 룰·6차 회의록 기입) ② **탭 딥링크** — `?tab=holdings|plan|apmp|trades|signals|calendar|notes`로 특정 탭 직행·공유 가능, 탭 전환 시 주소창 자동 갱신 + 마지막 탭 localStorage 복원(전 탭으로 확대). ⚠️ 컴포넌트의 `history` state가 `window.history`를 가리므로 반드시 `window.history.replaceState`로 쓸 것 ③ 투자 스토리 블록 접기(팀 모드만, 상태 기억 — 공개판은 항상 펼침) ④ 수익 기여 순위 전체 펼침. **대기**: EUAD 397주 매도 오늘 밤 집행 (회수 약 $17.4K, 손실 약 −7.3% — 협상 돌파구 없음 재확인, 절반 축소 유지).
- **2026-09-07 (메인 기기)** — 주간 포트 점검·수시 리밸런싱 분석. ① ~~삼전 24주·하이닉스 3주 익절 기록~~ → **9/8에 취소** (사용자 미승인 기록이었음 — 아래 매매 승인 규칙 참조) ② **EUAD 절반(397주) 축소 결정** — 우크라이나 휴전 협상 진전으로 재무장 촉매 약화(−7.3%, 시그널 약세). 9/8(화) 미국장 개장 시 집행 예약 ③ TLT·IEF 유지하되 **미 10년물 5.0% 상향 돌파 시 TLT 절반 축소 재검토** 룰 설정 ④ KRE 보류·VT 잔여 미집행 유지 — 차기 기수 신규 스토리 슬리브 재원. ⚠️ 6차(9/1) 회의 결과가 미기록 상태 — 개최됐다면 결정 사항을 회의록에 소급 기입할 것.
- **2026-08-31 (메인 기기)** — ① 투자 스토리 블록(보유 현황 상단)·플랜 종목별 편입 근거(`why`) 문구를 공식 보고서체로 통일, 코어·현금 역할은 "차기 기수 재원"으로 규정 ② 주간 운용보고 초안에 이번 주 매매(원장 7일)·회의 요약(최근 결정+차기 안건) 자동 삽입, 문장 단위 클리핑 ③ **잭슨홀(8/28) 결과 반영 — Warsh 매파(인상 가능성 시사)**: KRE 편입 보류 확정(이평선 아래·XLF와 디버전스), VT 잔여 $25K 미집행 유지. 둘 다 6차 안건 ⑦에 근거 기록. **보류**: VT 잔여·KRE — 9/1 6차에서 결정.
- **2026-08-28 (메인 기기)** — ① HPQ 421주 매수 기록(8/27 종가 29.63, 수시 집행 — 기술/펀더 근거는 체결 노트) ② 팀 모드에 "오늘의 분석" 탭 신설(시장 온도 + 종목별 규칙 시그널 + 관심종목) ③ 캘린더에 발표 결과 실제치·서프라이즈 표시(야후 우선·낫닥 폴백) + 월간 클릭 상세 ④ 외부 리서치 노트 4건 반입(`research/` — RBRK·CRWD·MRVL·ALM, 관심종목에 논지 표시) ⑤ **6차 회의 안건지 등록(9/1)** — 투자 스토리·VT 축소·알파 증액·role 확정. ⑥ **투자 스토리 블록 1차 구현**(보유 현황 상단, 1차 회의 섹터 프레임 기준 — 섹터→역할 매핑은 `StoryBlock.tsx` 상수, 6차에서 role·비중 확정되면 갱신) + **플랜 탭 종목별 매수 근거**(holdings.json `why` 필드, 행 클릭으로 펼침) — 선배 제출용 보완. **보류**: VT 잔여 $25K·KRE는 6차 결정까지 집행 금지. **다음 작업**: 6차 결정 반영(비중 조정 매매 + 역할별 도넛 토글).
## 데이터 원본

| 파일 | 역할 |
|---|---|
| `data/holdings.json` | 종목·섹터·**목표 비중(MP)**·목표 금액·상태. 보유 수량/평단의 기준 |
| `data/trades.json` | 체결 원장. 원가는 **체결 시점 환율로 고정**(오늘 환율로 재환산하지 않는다 — 그러면 환차손익이 화면에서 사라진다) |
| `data/kr-events.json`, `data/meeting-notes.json` | 캘린더·회의록 |
| `data/actions.json` | 팀 뷰 "오늘 할 일" 배너. **집행·결정 완료 시 항목 삭제, 새 예약·룰 추가** — 매매 기록과 함께 갱신할 것 |

시세는 `/api/quotes` (국내 네이버, 해외·환율·지표 Yahoo). 15~20분 지연 가능.

---

## 투자 스토리 (작성 대기 — 다음 작업)

피드백으로 받은 요구: **"우리가 메인으로 투자한 건 어떤 종목의 어떤 알파고, 그 수익을 얻기 위해
어떤 종목들에 투자했고, 이것을 헷징하기 위해 어떤 종목을 넣었다"가 대시보드에서 읽혀야 한다.**

MP 구조를 집계하면 스토리는 **이미 설계도에 들어 있다.** 대시보드가 말을 안 하고 있을 뿐이다.

### 현재 MP 구조 (합계 100%)

| 역할 | 슬리브 | MP | 구성 |
|---|---|---:|---|
| 코어 | 코어 인덱스 | 40.0% | VT |
| **메인 알파** | AI CapEx | 15.0% | MSFT · META · INTC · 삼성전자 · SK하이닉스 |
| 헤지 ① | 메모리 역상관 | 10.0% | QCOM · HPQ\* · AAPL · DELL |
| 분산 | 에너지 | 10.0% | XOM · CVX · SHEL · TTE |
| 분산 | 유럽 방산 | 7.5% | EUAD |
| 헤지 ② | 금리 (인하) | 7.5% | TLT · IEF |
| 헤지 ② | 금리 (인상) | 5.0% | JPM · BAC · KRE\* · XLF |
| 현금 | 현금 | 5.0% | SGOV |

\* 미매수

### 각 슬리브의 논리

- **메인 알파 — AI CapEx의 메모리 병목.** 공급 쪽(삼성전자·SK하이닉스)과 수요 쪽(MSFT·META)을
  같이 들어 사이클 전체를 먹는 구조.
- **헤지 ① 메모리 역상관.** QCOM·HPQ·AAPL·DELL은 메모리를 **사는** 쪽이라 메모리 값이 오르면
  원가 부담을 진다. 메인 베팅이 틀린 국면(메모리 하락)에서 방어하는 자리.
- **헤지 ② 금리 바벨.** 인상 수혜(은행) ↔ 인하 수혜(장기채)를 양쪽에 걸어 금리 방향을 중화.
  순 노출은 인하 쪽으로 2.5%p 기울어 있다.
- **분산.** 에너지·유럽 방산은 위 테마와 상관이 낮은 자리.
- **코어 + 현금.** VT는 시장 전체라 정의상 알파원이 될 수 없다. 견해가 없는 자본의 주차장이다.

### 2026-08-27 시점 성과 (한 달 치, 참고용)

- 총손익 AUM 대비 **+3.06%** / 코스피 원화 +22.1%, 달러환산 +28.4% / S&P 500 +4.9% → **벤치마크 하회**
- 기여도 상위 3개가 총손익의 **85%**: 삼성전자 33%, SK하이닉스 27%, MSFT 26%
- 한국(삼전+하이닉스)은 비중 7.7%로 총손익의 **57%** 생산. 원화 기준 수익률 +26.2%로
  코스피(+22.1%)를 **4.1%p 초과** — 종목선택 알파가 실재한다
- 최대 손실원: EUAD (−2.2%, 기여 −5.3%)
- **진단: 선택은 맞았고 배분이 어긋났다.** 맞힌 것에 7.7%만 실렸고, 40% 담기로 한 VT는 34%에
  그쳤고, 9%는 미집행 현금이었다.

⚠️ **한 달 데이터다.** "성과가 논리를 증명했다"로 쓰지 말 것.
"논리를 세웠고 첫 달은 이렇게 나왔다"까지가 정직한 표현이다.

### 구현 방향

`data/holdings.json`의 각 포지션에 **`role`** 필드 추가 (`알파` / `헤지` / `분산` / `코어` / `현금`).
그러면 ① 상단 스토리 블록(알파 → 표현 종목 → 헤지 장치 → 분산) ② 도넛에 역할별 토글 추가
(현재 종목별·섹터별 2축 → 3축) 가 데이터로 굴러간다.

**스토리 문구와 `role` 값은 팀의 판단이므로 임의로 정하지 말 것.** 위 구조를 초안으로 제시하고
팀이 확정하게 한다.

---

## VT / 현금 정책 (논의 진행 중, 미확정)

팀 의견: *"VT는 사실상 현금처럼 들고 있는 느낌이다. 월드 알파로 가져갈 거면 전체 포트의 10% 미만으로
줄이고, 현금 보유분은 SGOV로 편입하면 되지 않나."*

이에 대한 정리:

- **동의**: VT 40%는 견해가 아니라 결정 유보다. 포트의 40%가 아무 말을 안 하니 스토리가 안 읽힌다.
  VT에서 알파를 기대하는 건 정의상 불가능(시장 전체이므로). 10% 미만 + 역할을 "미배분 자본의 주차장"으로
  규정하는 게 논리적으로 맞다.
- **주의**: **VT는 현금이 아니다.** 100% 주식 베타다. 한 달간 +0.0%였던 건 성질이 아니라 우연이고,
  시장이 −15% 가면 VT도 −15% 간다. VT→SGOV는 라벨 정리가 아니라 **주식 노출 30%를 없애는
  대규모 리스크 축소**다.
- **진짜 결정은 빠진 30%의 행선지**:
  | 행선지 | 결과 |
  |---|---|
  | 전부 SGOV | 현금성 35%. 상승장에서 구조적으로 진다 |
  | 전부 테마 증액 | 스토리는 선명해지나 메모리 한 방에 성과가 결정됨 |
  | 혼합 (권고) | VT 10% / SGOV 5~8% / 나머지는 미체결 목표부터 집행 |
- 새 베팅보다 **설계도에 이미 있는데 실행이 안 된 자리(HPQ·KRE·VT 잔여)를 먼저 채우는** 게 순서다.

### 현금 정책 초안 (미확정)

하나의 숫자로 정하면 무너진다. 성격이 셋이라 나눠서 관리한다.

| 구분 | 목표 | 성격 |
|---|---|---|
| 정책 현금 | 3% (SGOV) | 리밸런싱·체결 여유. 상환 요구가 없는 모의 자금이라 유동성 니즈는 사실상 0 |
| 전술 현금 | 상한 +7% (합계 10%) | "지금은 살 게 없다"는 **적극적 판단**일 때만. 근거와 **해제 조건**을 회의록에 기록 |
| 미집행 | 목표 0% | 정책이 아니라 **집행 누락**. 현금 비중으로 세지 말고 분리 표기 |

- **총 상한 15%.** 넘기면 "시장 하락을 예상한다"는 명시적 액티브 콜로 간주하고 근거를 기록.
- 근거: SGOV 캐리 연 4%대 → 현금 10%의 기회비용은 주식 기대 8% 가정 시 **연 0.4%p 드래그**.
  20% 넘으면 0.8%p 이상 + 트래킹 에러 확대로 학회 포트에서 정당화가 어렵다.
- **리밸런싱 밴드는 절대값(±1%p)이 아니라 목표의 ±25%.** 1.3%짜리 자리에 ±1%p를 걸면
  사실상 밴드가 없다. (`components/Allocation.tsx`의 `BAND` 상수)

---

## 구현 현황

- ✅ **AP·MP 탭** (`components/Allocation.tsx`) — MP / AP / 괴리 / 기여도.
  괴리를 **미집행 · 집행 부족 · 드리프트 · 정상**으로 분류한다. 숫자가 같아도 할 일이 정반대라서다.
  미집행 현금은 별도 행. 비중 분모는 **총자산(평가액+현금)** 이며, 히어로의 "AUM 대비"와는 분모가 다르다.
  ⚠️ 미체결 목표와 가용 현금은 **절대 합산하지 말 것** — 현금은 그 목표를 채울 재원이라
  합치면 "가진 돈보다 많이 사야 한다"는 뜻이 된다.
- ⬜ 투자 스토리 블록 (위 "구현 방향")
- ⬜ `role` 축 도넛 토글
- ⬜ 현금 3분할(정책/전술/미집행) 표기

## 알려진 문제

- `components/Logo.tsx`가 SSR/CSR 간 style 객체 표기가 달라 **hydration mismatch 경고**를 낸다
  (TTE 등 로고 이미지가 없어 모노 이니셜로 떨어지는 종목). 화면은 정상이라 방치 중.
